# Optional local dependencies for presenter-cutout B-roll templates.
# Install with: python3 -m pip install -r requirements-matting.txt
# Newer MediaPipe wheels use the Tasks API. Video Cleaner downloads Google's
# official selfie-segmenter model into ~/.video-cleaner/models on first use.
numpy>=1.26,<3
opencv-python-headless>=4.9,<5
mediapipe>=0.10.14,<0.11
